<Window x:Class="Aevor.UI.Views.ConfirmRestoreWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Confirm Restore" Height="240" Width="440"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    
    <Border Margin="16" Background="White" CornerRadius="12" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="315" ShadowDepth="1" Opacity="0.15" Color="Black" />
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <!-- Header -->
                <RowDefinition Height="*" />    <!-- Content -->
                <RowDefinition Height="Auto" /> <!-- Footer/Buttons -->
            </Grid.RowDefinitions>
            
            <!-- Header Grid (Draggable) -->
            <Grid x:Name="HeaderGrid" Grid.Row="0" Background="Transparent" MouseLeftButtonDown="HeaderGrid_MouseLeftButtonDown" Height="52">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal" Margin="16,12,0,4">
                    <Image Source="{StaticResource AevorIcon}" Width="24" Height="24" RenderOptions.BitmapScalingMode="HighQuality" VerticalAlignment="Center"/>
                    <TextBlock Text="Confirm Restore" Style="{StaticResource H3TextBlockStyle}" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>
                
                <!-- Close Button -->
                <Button Grid.Column="1" Style="{StaticResource GhostButton}" Width="32" Height="32" Margin="0,4,12,0" Padding="0" Click="CloseButton_Click" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Path Data="M0,0 L10,10 M10,0 L0,10" 
                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" 
                          StrokeThickness="1.5" 
                          HorizontalAlignment="Center" 
                          VerticalAlignment="Center" 
                          Width="10" 
                          Height="10" />
                </Button>
            </Grid>
            
            <!-- Content -->
            <Grid Grid.Row="1" Margin="24,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <!-- Info / Warning Icon (Amber theme for restore caution) -->
                <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20" Background="#FFFBEB" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Path Data="M12,9 L12,13 M12,17 L12.01,17 M12,2 C6.48,2 2,6.48 2,12 C2,17.52 6.48,22 12,22 C17.52,22 22,17.52 22,12 C22,6.48 17.52,2 12,2 Z" 
                          Stroke="#F59E0B" 
                          StrokeThickness="2" 
                          StrokeStartLineCap="Round" 
                          StrokeEndLineCap="Round" 
                          HorizontalAlignment="Center" 
                          VerticalAlignment="Center" 
                          Width="20" 
                          Height="20" 
                          Stretch="Fill"/>
                </Border>
                
                <!-- Message Text -->
                <TextBlock Grid.Column="1" x:Name="MessageText" 
                           Style="{StaticResource BodyTextBlockStyle}" 
                           TextWrapping="Wrap" 
                           VerticalAlignment="Center" 
                           Margin="16,0,0,0" />
            </Grid>
            
            <!-- Footer -->
            <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}" CornerRadius="0,0,11,11" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource BorderBrush}" Padding="24,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Width="90" Height="34" Margin="0,0,8,0" Click="CloseButton_Click" IsCancel="True" />
                    <Button Content="Restore" Style="{StaticResource PrimaryButton}" Width="90" Height="34" Click="ConfirmButton_Click" IsDefault="True" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>


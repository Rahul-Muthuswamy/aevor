<Window x:Class="Aevor.UI.Views.ConfirmDeleteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Confirm Delete" Height="240" Width="440"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Border Margin="16" Background="White" CornerRadius="12" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="315" ShadowDepth="1" Opacity="0.15" Color="Black" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid x:Name="HeaderGrid" Grid.Row="0" Background="Transparent" MouseLeftButtonDown="HeaderGrid_MouseLeftButtonDown" Height="52">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" Margin="16,12,0,4">
                    <Image Source="{StaticResource AevorIcon}" Width="24" Height="24" RenderOptions.BitmapScalingMode="HighQuality" VerticalAlignment="Center"/>
                    <TextBlock Text="Confirm Delete" Style="{StaticResource H3TextBlockStyle}" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>

                <Button Grid.Column="1" Style="{StaticResource GhostButton}" Width="32" Height="32" Margin="0,4,12,0" Padding="0" Click="CloseButton_Click" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Path Data="M0,0 L10,10 M10,0 L0,10"
                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                          StrokeThickness="1.5"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Width="10"
                          Height="10" />
                </Button>
            </Grid>

            <Grid x:Name="MessagePanel" Grid.Row="1" Margin="24,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Width="40" Height="40" CornerRadius="20" Background="#FEF2F2" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Path Data="M12,9 L12,13 M12,17 L12.01,17 M12,2 C6.48,2 2,6.48 2,12 C2,17.52 6.48,22 12,22 C17.52,22 22,17.52 22,12 C22,6.48 17.52,2 12,2 Z"
                          Stroke="#EF4444"
                          StrokeThickness="2"
                          StrokeStartLineCap="Round"
                          StrokeEndLineCap="Round"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Width="20"
                          Height="20"
                          Stretch="Fill"/>
                </Border>

                <TextBlock Grid.Column="1" x:Name="MessageText"
                           Style="{StaticResource BodyTextBlockStyle}"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"
                           Margin="16,0,0,0" />
            </Grid>

            <Grid x:Name="LoadingPanel" Grid.Row="1" Visibility="Collapsed" Margin="24,12,24,12">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Image Source="{StaticResource AevorIcon}" Width="44" Height="44" RenderOptions.BitmapScalingMode="HighQuality" RenderTransformOrigin="0.5,0.5" HorizontalAlignment="Center">
                        <Image.RenderTransform>
                            <RotateTransform x:Name="SpinnerRotate" Angle="0" />
                        </Image.RenderTransform>
                        <Image.Style>
                            <Style TargetType="Image">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Visibility, ElementName=LoadingPanel}" Value="Visible">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard x:Name="RotationStoryboard">
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="(Image.RenderTransform).(RotateTransform.Angle)"
                                                                     From="0" To="360" Duration="0:0:1.5"
                                                                     RepeatBehavior="Forever" />
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <StopStoryboard BeginStoryboardName="RotationStoryboard" />
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Image.Style>
                    </Image>
                    <TextBlock Text="Deleting backup..." Style="{StaticResource BodyTextBlockStyle}" HorizontalAlignment="Center" Margin="0,12,0,0" FontWeight="SemiBold"/>
                    <TextBlock Text="Please wait while the files are removed" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{DynamicResource MutedBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>

            <Border x:Name="FooterBorder" Grid.Row="2" Background="{DynamicResource SurfaceBrush}" CornerRadius="0,0,11,11" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource BorderBrush}" Padding="24,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Width="90" Height="34" Margin="0,0,8,0" Click="CloseButton_Click" IsCancel="True" />
                    <Button Content="Delete" Style="{StaticResource DangerButton}" Width="90" Height="34" Click="ConfirmButton_Click" IsDefault="True" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>

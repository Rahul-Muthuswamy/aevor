<Window x:Class="Aevor.UI.Views.CreateBackupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Create Backup" Height="260" Width="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    
    <Border Margin="16" Background="White" CornerRadius="12" BorderThickness="1" BorderBrush="{StaticResource BorderBrush}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Direction="315" ShadowDepth="1" Opacity="0.15" Color="Black" />
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <!-- Header -->
                <RowDefinition Height="*" />    <!-- Content -->
                <RowDefinition Height="Auto" /> <!-- Footer/Buttons -->
            </Grid.RowDefinitions>
            
            <!-- Header Grid (Draggable) -->
            <Grid x:Name="HeaderGrid" Grid.Row="0" Background="Transparent" MouseLeftButtonDown="HeaderGrid_MouseLeftButtonDown" Height="52">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal" Margin="16,12,0,4">
                    <Image Source="{StaticResource AevorIcon}" Width="24" Height="24" RenderOptions.BitmapScalingMode="HighQuality" VerticalAlignment="Center"/>
                    <TextBlock Text="Create Backup" Style="{StaticResource H3TextBlockStyle}" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>
                
                <!-- Close Button -->
                <Button Grid.Column="1" Style="{StaticResource GhostButton}" Width="32" Height="32" Margin="0,4,12,0" Padding="0" Click="CloseButton_Click" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Path Data="M0,0 L10,10 M10,0 L0,10" 
                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" 
                          StrokeThickness="1.5" 
                          HorizontalAlignment="Center" 
                          VerticalAlignment="Center" 
                          Width="10" 
                          Height="10" />
                </Button>
            </Grid>
            
            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="24,12,24,12">
                <TextBlock Text="Select the Brave profile you want to back up:" 
                           Style="{StaticResource BodySmallTextBlockStyle}" 
                           Foreground="{StaticResource MutedBrush}"
                           Margin="0,0,0,8" />
                           
                <!-- ComboBox styled with custom resources -->
                <ComboBox x:Name="ProfileComboBox" Height="38" VerticalContentAlignment="Center" FontSize="13" />
            </StackPanel>
            
            <!-- Footer -->
            <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" CornerRadius="0,0,11,11" BorderThickness="0,1,0,0" BorderBrush="{StaticResource BorderBrush}" Padding="24,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Width="90" Height="34" Margin="0,0,8,0" Click="CloseButton_Click" IsCancel="True" />
                    <Button Content="Backup" Style="{StaticResource PrimaryButton}" Width="90" Height="34" Click="BackupButton_Click" IsDefault="True" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
